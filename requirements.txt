# Data manipulation
pandas==2.2.3
numpy==1.26.4

# Jupyter notebook support
jupyter==1.1.1

# Optional: PySpark for local Spark pipelines
pyspark==3.5.0

# Optional: SQLite connector (comes with Python, no need to install)
# Optional: requests for API examples
requests==2.32.0
